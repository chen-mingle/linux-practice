#Week01 Linux基础笔记
2026-09-07

##一、目录结构
"/"为根目录，所有文件目录都是从根目录开始
HOME目录 是每个用户的个人账户目录，路径在/home/用户名
绝对路径：以根目录为起点
相对路径：以当前目录为起点

##二、命令基础
command [-options] [parameter]
command：命令本身
-options：命令的选项，控制命令的行为细节（非必填）
parameter：命令的参数，多数用于命令的指向目标（非必填）

##三、基础命令
- ls [-a -l -h] [Linux路径]
-a -l -h 是可选选项；Linux路径 是可选参数
· -a 表示all，列出全部的文件（包含隐藏的文件/文件夹），以.开头的就是隐藏的文件;
· -l 表示以竖向排列，并展示更多信息
· -h 表示以易于阅读的形式，列出文件的大小，如K(B)、M(B)、G(B)，必须以-lh混合使用
也可以组合使用：
· ls -l -a
· ls -la
· ls -al
· ls -l -a -h
· ls -lah
这三种写法都是相同的作用

- cd [Linux路径]
（change directory）用来切换工作目录
· . 表示当前目录，比如cd ./Desktop表示切换到当前目录的Desktop目录下，和cd Desktop效果一样
· .. 表示上一级目录，cd ../..表示切换到上两级的目录
· ~ 表示HOME目录

- pwd
（print work directory）查看当前的工作目录

- mkdir [-p] Linux路径
（make directory）创建新的目录（文件夹）
· -p 可选，表示自动创建不存在的父目录

- touch Linux路径
在对应的路径下创建文件

- cat Linux路径
查看对应路径的文件，全部显示

- more Linux路径
查看对应路径的文件，可翻页，空格翻页，Q退出

- cp [-r] 参数1 参数2
用于复制文件或文件夹
· -r 可选，用于复制文件夹使用，表示递归
· 参数1,Linux路径，表示被复制的文件或文件夹
· 参数2,Linux路径，表示要复制去的地方

- mv 参数1 参数2
用于移动或改名文件或文件夹
· 参数1,Linux路径，表示被移动的文件或文件夹
· 参数2,Linux路径，表示要移动去的地方，如果目标不存在，则进行改名

- rm [-r -f] 参数1 参数2 ...... 参数N
· -r 用来删除文件夹
· -f 表示force，强制删除（不会弹出提示信息），一般在root超级管理员用户下使用
· 参数N 表示要删除的文件或文件夹路径，以空格隔开
支持通配符*

- *
表示通配符，即匹配任意内容（包含空）
· test*，表示匹配任何以test开头的内容
· *test，表示匹配任何以test结尾的内容
· *test*，表示匹配任何包含test的内容

- su - root
切换超级管理员用户
· rm -rf /
· rm -rf/*
删除根目录，等同于在Windows上执行C盘格式化

- which 要查找的命令
查找命令的程序文件位置

- find 起始路径 -name "被查找文件名"
查找指定的文件
支持通配符

- find 起始路径 -size +|-n[kMG]
· +、-表示大于和小于
· n表示大小数字
· kMG表示大小单位，k（小写）表示kb，M表示MB，G表示GB

- grep [-n] 关键字 文件路径
在文件中通过关键字过滤文件行
· -n，可选，表示在结果中显示匹配的行的行号
· 关键字，表示过滤的关键字，如带有空格或其他特殊符号，建议使用" "将关键字包起来
· 文件路径，表示要过滤内容的文件路径，可作为内容输入端口

- wc [-c -m -l -w] 文件路径
用来统计文件的行数、单词数量等
· -c，统计bytes数量
· -m，统计字符数量
· -l，统计行数
· -w，统计单词数量
· 文件路径，被统计的文件，可作为内容输入端口
如不加选项，以行数 单词数 字节数 文件名的顺序统计

- | 
表示管道符，将符号左边的命令的结果作为右边命令的输入
· cat test.text | grep itheima，在test.text中过滤"itheima"

- echo 输出的内容
用于在命令行输出指定的内容，如有空格等特殊符号，要使用" "包围
· 使用``包围的内容将被当作命令执行

- >
重定向符
· >，将左侧命令的结果，覆盖写入到右侧制定的文件中
· >>，将左侧命令的结果，追加写入到右侧的指定的文件中

- tail [-f -num] Linux路径
用于查看文件尾部的内容，跟踪文件的最新更改
· Linux路径，表示被跟踪的文件路径
· -f，表示持续跟踪
· -num，表示查看尾部多少行，不填默认10行

- ln -s 参数1 参数2
用于创建软连接，可将文件、文件夹链接到其他位置
· -s，创建软连接
· 参数1,被链接的文件或文件夹
· 参数2,要链接去的目的地

- date[-d] [+格式化字符串]
· -d，按照给定的字符串显示日期，一般用于日期计算
· 格式化字符串，通过特定的字符串标记，来控制显示的日期格式
  · %Y，年
  · %y，年份后两位数字（00..99）
  · %m，月份（01..12）
  · %d，日（01..31）
  · %H，小时（00..23）
  · %M，分钟（00..59）
  · %S，秒（00..60）
  · %s，自1970-01-01 00:00:00 UTC 到现在的秒数
· 修改时区，使用root权限
  · rm -f /etc/localtime
  · ln -s /usr/share/zoneinfo/Asia/Shanghai/ /etc/localtime

- ifconfig
用于查看ip地址等配置

- hostname
用于查看主机名
· - hostnamectl set-hostname 主机名，修改主机名（需root）

- ping [-c num] ip或主机名
· -c，检查的次数，如不加此选项，将无限次检查
· ip或主机名，被检查的ip地址或主机名地址

- wget [-b] url
使用wget下载器下载文件，可通过tail -f wget.log查看下载进度
· -b，可选，后台下载，会将日志写入到当前工作日志wget-log
· url，下载链接

- curl [-O] url
· -O，用于下载文件，当url是下载链接时，可以使用此选项保存文件
· url，要发起请求的网络地址

- nmap 被查看的ip地址
用于查看此ip地址的端口占用情况

- netstat -anp | grep 端口号
用于查看指定端口的占用情况

- ps [-e -f]
· -e，显示出全部的进程
· -f，以完全格式化的形式展示信息（展示全部信息）
ps -ef | grep tail，过滤tail关键字的进程

- kill [-9] 进程ID
用于关闭进程
· -9,表示强制关闭进程

- top
用于查看CPU、内存使用情况，按q或ctrl+c退出
· -p，只显示某个进程的信息
· -d，设置刷新时间，默认是5秒
· -c，显示产生进程的完整命令，默认是进程名
· -n，指定刷新次数，比如 top -n 3，刷新3次后退出
· -b，以非交互非全屏模式运行，以批次的方式执行top，一般配合-n指定输出几次统计信息，将输出重定向到指定文件
· -i，不显示任何闲置或无用进程
· -u，查找特定用户启动的进程

- df [-h]
用于查看硬盘的使用情况
· -h，以更加人性化的单位显示

- iostat [-x] [num1] [num2]
用于查看磁盘信息
· -x，显示更多信息
· num1,数字，刷新间隔
· num2,数字，刷新几次

- sar -n DEV num1 num2
用于监控2网络状态
· -n，查看网络，DEV表示查看网络接口
· num1，刷新间隔（不填就查看一次结束）
· num2,查看次数（不填就是无限次数）

- env
用于查看环境变量，环境变量都是KeyValue格式

- $
用于取“变量”的值
· echo $PATH，取得PATH的值
· echo ${PATH}ABC，取得PATH的值再加上ABC

- export 变量名=变量值
用于自行设置环境变量
· 针对当前用户，配置在当前用户的：~/.bashrc文件中
· 针对所有用户，配置在系统的：/etc/profile文件中
· 并通过语法：source配置文件，进行立即生效，或重新登陆FInalShell

- rz
用于上传

- sz
用于下载

- tar [-c -v -x -f -z -C] 参数1 参数2 ... 参数N
用于压缩或解压
· -c，创建压缩文件
· -v，显示压缩、解压过程
· -x，解压模式
· -f，要创建的文件，或要解压的文件，-f选项必须在所有选项的最后
· z，gzip模式，不使用-z就是普通的tarball格式
· -C，选择解压的目的地，用于解压模式
· tar -cvf test.tar 1.txt 2.txt 3.txt，将1.txt 2.txt 3.txt压缩到test.tar文件内
· tar -zcvf test.tar.gz 1.txt 2.txt 3.txt，将1.txt 2.txt 3.txt压缩到test.tar.gz文件内
· tar -xvf test.tar -C，将test.tar解压到当前目录内
· tar -xvf teat.tar -C /home/xxx/，将test.tar解压到对应目录内
· tar -zxvf teat.tar.gz -C /home/xxx/，将test.tar.gz解压到对应目录内

- zip [-r] 参数1 参数2 ... 参数N
· -r，被压缩的包含文件夹的时候，需要选用-r选项，和rm、cp等命令的-r效果一致

- unzip [-d] 参数
· -d，指定要解压去的位置，同tar的-C选项
· 参数，被解压的zip压缩包文件



##四、vi/vim编辑器
- 三种工作模式

· 命令模式
命令模式下，所敲的按键编辑器都理解为命令，以命令驱动执行不同的功能。此模式下，不能自由进文本辑
· 输入模式
编辑模式、插入模式，此模式下，可以对文件内容进行自由编辑
· 底线命令模式
以:开始，通常用于文件的保存、退出

使用vi/vim filename进入命令模式，使用wq退出
在命令模式下：
- 输入i/a/o进入输入模式，使用ESC退回命令模式
· i，在当前光标位置进入输入模式
· a，在当前光标位置之后进入输入模式
· I，在当前行的开头进入输入模式
· A，在当前行的结尾进入输入模式
· o，在当前光标下一行进入输入模式
· O，在当前光标上一行进入输入模式
- 输入“ : ”进入底线命令模式，以回车结束运行
· :wq，保存并退出
· :q，仅退出
· :q!，强制退出
· :w，仅保存
· :set nu，显示行号
· :set paste，设置粘贴模式

- vi 文件路径
- vim 文件路径
vim兼容全部的vi功能
· 如果文件路径表示的文件不存在，那么此命令会用于编辑新文件
· 如果文件路径表示的文件存在，那么此命令用于编辑已有的文件

##五、用户权限
root，超级管理员
- su [-] [用户名]
用于切换用户
· 用户名，表示要切换的用户，省略表示切换到root
· 切换之后，可以使用exit命令退回上一个用户，也可使用ctrl+d

- sudo 其它命令
· 为这一命令临时赋予root权限

- rwx
· r，表示读权限（read）
· w，表示写权限（write）
· x，表示执行权限（execute）

- chmod [-R] 权限 文件或文件夹
用于修改文件、文件夹的权限信息
· -R，对文件夹内的全部内容应用同样的操作

- chmod 751 文件或文件夹
也可用数字来代表权限
r记作4,w记作2,x记作1
· 0：无权限，---
· 1：仅有x权限，--x
· 2：仅有w权限，-w-
· 3：有w和x权限，-wx
· 4：仅有r权限，r--
· 5：有r和x权限，r-x
· 6：有r和w权限，rw-
· 7：有全部权限，rwx

- chown [-R] [用户] [:] [用户组] 文件或文件夹
只适用于root用户，用来修改所属用户或用户组
· -R，对文件夹内的全部内容应用同样的操作
· 用户，修改所属用户
· 用户组，修改所属用户组
· :，用于分隔用户和用户组

##六、用户、用户组
需root用户执行
- groupadd 用户组名
创建用户组

- groupdel 用户名
删除用户组

- useradd[-g -d] 用户名，添加用户
· -g，指定用户的组，不指定-g会创建同名组并自动加入，指定-g需要组已经存在，如已存在，如已存在同名组，必须使用-g
· -d，指定用户HOME路径，不指定，HOME目录默认在：/home/用户名

- userdel [-r] 用户名，删除用户
· -r，删除用户的HOME目录，不使用-r，删除用户时，HOME目录保留

- id [用户名]，查看用户所属组
· 用户名，被查看的用户，如不提供则查看自身

- usermod -aG 用户组 用户名，将指定用户加入指定用户组

- getent passwd
用来查看当前系统中有哪些用户
包含：用户名:密码(x)用户ID:组ID:描述信息:HOME目录:执行终端（默认bash）

- getent group
查看当前系统中有哪些用户组
包含：组名称:组认证（显示为x）:组ID
















